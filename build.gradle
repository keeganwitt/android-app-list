buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.9.0'
    }
}

tasks.register('clean', Delete) {
    delete rootProject.Project.layout.buildDirectory
}
